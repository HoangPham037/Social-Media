<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SocialMedia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="popupMenuBackground">@drawable/menu_bgr</item>
         <item name="colorPrimary">@color/color_orange_app</item>
        <item name="android:fontFamily">@font/jakatavariable</item>
    </style>

    <style name="Theme.SocialMedia" parent="Base.Theme.SocialMedia">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>